<% content_for :title, "New sound" %>

<div class="mx-auto max-w-2xl px-4 py-8">
  <h1 class="text-2xl font-semibold text-slate-900">New sound</h1>
  <p class="mb-6 text-sm text-slate-500">Upload an MP3 for one of the four music slots.</p>

  <%= render "form", sound: @sound %>
</div>
